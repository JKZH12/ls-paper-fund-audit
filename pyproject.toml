[project]
name = "paper-portfolio"
version = "0.1.0"
description = "Local paper portfolio ledger for a simulated long/short hedge fund."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
